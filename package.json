{
  "name": "makibishi-ranking-bot",
  "version": "0.1.0",
  "description": "makibishi ranking bot",
  "keywords": [
    "nostr"
  ],
  "main": "index.js",
  "license": "CC0-1.0",
  "author": {
    "name": "Don"
  },
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "crawl": "node --import \"data:text/javascript,import { register } from 'node:module'; import { pathToFileURL } from 'node:url'; register('ts-node/esm', pathToFileURL('./'));\" ./src/main.ts",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "type": "module",
  "dependencies": {
    "nostr-tools": "^2.15.0"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
